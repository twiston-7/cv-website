import React from "react"
import ReactDOM from "react-dom/client"
import InitialView from "./components/initial-view/InitialView.tsx"

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <InitialView />
  </React.StrictMode>,
)